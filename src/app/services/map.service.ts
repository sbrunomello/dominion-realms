import { Injectable } from '@angular/core';
//import { Firestore, collection, doc, getDoc } from '@angular/fire/firestore';
//import { mockRegions, mockCities, mockCityDetails } from '../../app/services/mock.service';


@Injectable({
  providedIn: 'root',
})
export class MapService {
  /*async getMockRegions(): Promise<any[]> {
    return mockRegions; // Substitui flat()
  }

  async getMockCities(): Promise<any[]> {
    return mockCities.reduce((acc, val) => acc.concat(val), []); // Substitui flat()
  }

  async getMockBuildings(): Promise<any[]> {
    return mockCityDetails.reduce((acc, val) => acc.concat(val), []); // Substitui flat()
  }*/
}

